---
import { getCollection } from "astro:content";
const products = await getCollection("products");
---

<ul>
    {
        products.map((product) => (
            <li key={product.data.id}>{product.data.title}</li>
        ))
    }
</ul>
